{
  "name": "lcim",
  "version": "2.0.1",
  "private": true,
  "description": "Low Cost Implementation Model — V2 controller (2.0.1: automatic GPT-5.6 Sol XHIGH review via Pi openai-codex OAuth)",
  "type": "module",
  "license": "UNLICENSED",
  "engines": {
    "node": ">=22.19.0"
  },
  "dependencies": {
    "@earendil-works/pi-coding-agent": "0.84.1"
  },
  "bin": {
    "lcim": "bin/lcim.mjs"
  },
  "scripts": {
    "test": "node --test \"tests/**/*.test.mjs\"",
    "test:smoke": "node --test \"tests/smoke/*.test.mjs\"",
    "test:unit": "node --test \"tests/unit/*.test.mjs\"",
    "test:guards": "node --test \"tests/guards/*.test.mjs\"",
    "test:audit": "node --test \"tests/audit/*.test.mjs\"",
    "test:compat:v1": "node --test \"tests/compat/v1/*.test.mjs\"",
    "test:integration": "node --test \"tests/integration/*.test.mjs\"",
    "test:cli": "node --test \"tests/smoke/*.test.mjs\" \"tests/integration/*.test.mjs\"",
    "check": "npm test"
  }
}
