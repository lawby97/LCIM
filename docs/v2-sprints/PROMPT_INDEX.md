
# Sprint prompt index

Each sprint file contains the authoritative full prompt. The short launcher is:

```text
Read docs/v2-sprints/00_MASTER_PLAN.md and docs/v2-sprints/<SPRINT_FILE>.md completely. Execute that sprint as an implementation task, obey its owned-file boundaries and acceptance criteria, run the required tests, do not commit/push/merge, and return the required sprint completion report.
```

Use the exact sprint-specific prompt at the bottom of the sprint file when available.
